<Window x:Class="OpenSpeaker.Views.ExtensionAuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="Height" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Bg1}"
        Foreground="{StaticResource TextPrimary}">
    <Window.Title>
        <Binding Path="EngineName" StringFormat="Configure - {0}"/>
    </Window.Title>
    <StackPanel Margin="24">
        <TextBlock Text="AUTHENTICATION" Style="{StaticResource SectionLabel}" Margin="0,0,0,12"/>

        <ItemsControl ItemsSource="{Binding Fields}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Label}"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource TextSecondary}"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                 FontFamily="Consolas"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Padding="20,7" Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"/>
            <Button Content="Save" Padding="20,7"
                    Style="{StaticResource AccentButton}"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </StackPanel>
</Window>
